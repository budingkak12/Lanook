前端页面 / 路由入口速查（给同事快速定位代码）

说明：
1) 本项目用 Next.js App Router：访问路径 = `webclient/app/**/page.tsx`
2) “首页 4 个 Tab”共用壳：`webclient/app/(main)/layout.tsx:18`（viewToPath 映射）

一、初始化页（不是独立 route，是 (main) layout 条件渲染）
- 访问方式（强制进入）：http://localhost:3000/?forceInit=true
- 触发逻辑：`webclient/app/(main)/layout.tsx:64`（查 `/init-status`）与 `layout.tsx:114`（forceInit）
- 页面组件：`webclient/components/initialization-view.tsx:22`
- 常用调试参数：
  - `?initStep=3`：直接跳到初始化第 3 步（`webclient/components/initialization-view.tsx:58`）
  - `?mock=1`：跳过初始化检查（`webclient/app/(main)/layout.tsx:38`）

二、首页四个 Tab（路径 -> 入口 page -> 主要组件）
- 随机流（Feed）：http://localhost:3000/
  - `webclient/app/(main)/page.tsx:9`
  - 主要渲染：`MediaCollectionView` + `MediaGrid`（`webclient/app/(main)/page.tsx:36`）

- 相册（Albums）：http://localhost:3000/albums
  - `webclient/app/(main)/albums/page.tsx:5`
  - 主要渲染：`webclient/components/albums-view.tsx`

- 搜索（Search）：http://localhost:3000/search
  - `webclient/app/(main)/search/page.tsx:10`
  - Tab 切换逻辑：`webclient/app/(main)/search/page.tsx:14`（`tab=advanced` / `advanced=1`）
  - 默认：`SearchIntentView`；高级：`AdvancedSearchView`（`webclient/app/(main)/search/page.tsx:74`）

- 设置（Settings）：http://localhost:3000/settings
  - `webclient/app/(main)/settings/page.tsx:5`
  - 主要渲染：`webclient/components/settings-view.tsx:26`

三、非 Tab 页面（调试/原型/组件预览）
- 组件预览页：http://localhost:3000/ui-demo
  - `webclient/app/ui-demo/page.tsx:6`
  - 内容组件：`webclient/components/ui-demo-view.tsx:26`

- 搜索 demo 页（如果还在用）：http://localhost:3000/searchv2
  - `webclient/app/(main)/searchv2/page.tsx:5`（`SearchIntentView` 的 demo variant）

- 原型页（如果还在用）：http://localhost:3000/testa
  - `webclient/app/testa/page.tsx:1`（StepNavigation/StepContent 的原型页面）

四、Tab 导航组件位置（找“点击 Tab 去哪”）
- PC 左侧栏：`webclient/components/main-sidebar.tsx:18`
- 移动端底部栏：`webclient/components/mobile-bottom-nav.tsx:12`

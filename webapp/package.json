{
  "name": "webapp",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "echo \"Use: npm run test:e2e (Playwright)\"",
    "test:e2e": "playwright test",
    "test:e2e:online": "E2E_ONLINE=1 playwright test",
    "test:e2e:offline": "E2E_OFFLINE=1 playwright test",
    "playwright:install": "playwright install --with-deps",
    "cap:init": "npx cap init mytikt com.mytikt.app --web-dir=dist --npm-client npm || true",
    "cap:add:android": "npx cap add android",
    "cap:copy": "npm run build && npx cap copy",
    "cap:sync": "npm run build && npx cap sync",
    "android:open": "npx cap open android",
    "android:build": "cd android && ./gradlew assembleDebug"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "module",
  "dependencies": {
    "react": "^19.1.1",
    "react-dom": "^19.1.1",
    "@capacitor/core": "^6.1.1",
    "@capacitor/android": "^6.1.1"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.1.1",
    "@playwright/test": "^1.46.0",
    "@types/react": "^19.1.15",
    "@types/react-dom": "^19.1.9",
    "@vitejs/plugin-react": "^4.3.2",
    "typescript": "^5.9.2",
    "vite": "^5.4.10"
  }
}
